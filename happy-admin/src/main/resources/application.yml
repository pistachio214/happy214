server:
  port: 14001

spring:
  application:
    name: happy211-admin-api

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/happy214?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 12345678

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false

  redis:
    host: 127.0.0.1
    port: 6379
    database: 2
    password: xiaofeng

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    auto-mapping-behavior: full
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:mapper/**Mapper.xml
  global-config:
    # 逻辑删除配置
    db-config:
      # 删除前
      logic-not-delete-value: 1
      # 删除后
      logic-delete-value: 0

happy:
  jwt:
    # 加密秘钥
    secret: f4e2e52034348f86b67cde581c0f9eb5
    # token有效时长，7天，单位秒
    expire: 604800
    header: Authorization
  security:
    # 设置不需要验证的路径
    matchers: "/webjars/**,/favicon.ico,/sys-auth/captcha,/login,/test,/doc.html**,/swagger-resources,/common/**,/v2/api-docs**"
    admin-login: "/login"
    admin-logout: "/logout"

minio:
  endpoint: http://123.57.48.127:9000
  accessKey: admin
  secretKey: admin123456
  bucketName: mlcloud

logs:
  path: logs